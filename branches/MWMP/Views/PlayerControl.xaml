<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:local="clr-namespace:MWMP"
	xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
	mc:Ignorable="d"
	x:Class="MWMP.PlayerControl"
	x:Name="UserControl">
    <UserControl.Resources>
        <ObjectDataProvider MethodName="GetService" ObjectType="{x:Type local:ModuleManager}" x:Key="MusicPlayer">
            <ObjectDataProvider.MethodParameters>
                <system:String>MusicPlayerViewModel</system:String>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
	<Grid x:Name="LayoutRoot" DataContext="{StaticResource ResourceKey=MusicPlayer}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="120"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="40"/>
		</Grid.RowDefinitions>
		<MediaElement Grid.Row="0" Grid.Column="0" x:Name="MediaPlayer" Source="{Binding Source, Mode=OneWay}" LoadedBehavior="Manual" 
			UnloadedBehavior="Manual"  Volume="{Binding Volume, Mode=OneWay}">
			<i:Interaction.Triggers>
				<i:EventTrigger EventName="MediaFailed">
                    <ei:CallMethodAction MethodName="LoadedMediaFaild" TargetObject="{Binding Source={StaticResource MusicPlayer}}" />
				</i:EventTrigger>
			</i:Interaction.Triggers>
		</MediaElement>
		<ListView x:Name="PlayList" Grid.Row="0" DataContext="{Binding PlayList}" Grid.Column="1" ItemsSource="{Binding Collection}" HorizontalAlignment="Left" SelectedIndex="{Binding CurrentMedia}">
            <ListView.View>
                <GridView AllowsColumnReorder="False">
                    <GridView.ColumnHeaderContainerStyle>
                        <Style>
                            <Setter Property="UIElement.Visibility" Value="Collapsed"/>
                        </Style>
                    </GridView.ColumnHeaderContainerStyle>
                    <GridViewColumn DisplayMemberBinding="{Binding Title}"/>
                </GridView>
            </ListView.View>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction  Command="{Binding Source={StaticResource ResourceKey=MusicPlayer}, Path=Open}" CommandParameter="{Binding ElementName=PlayList, Path=SelectedItem}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ListView>
		<StackPanel  Grid.Row="1" VerticalAlignment="Bottom">
			<StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
				<Slider Margin="10,0" Width="500" Maximum="{Binding DurationOnCurrentPlay, Mode=OneWay}" Value="{Binding PosOnCurrentPlay, Mode=OneWay}" x:Name="TimeSlider" IsMoveToPointEnabled="True">
					<i:Interaction.Triggers>
						<i:EventTrigger EventName="ValueChanged">
							<i:InvokeCommandAction Command="{Binding SeekToMediaPosition}" CommandParameter="{Binding ElementName=TimeSlider, Path=Value}"/>
						</i:EventTrigger>
					</i:Interaction.Triggers>
				</Slider>
				<TextBlock Text="{Binding Time, Mode=OneWay}"/>
			</StackPanel>
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
				<Button x:Name="PreviousButton" Content="¨Previous" Command="{Binding Previous}" />
				<Button x:Name="PlayButton" Content="Play" Command="{Binding Play}" />
				<Button x:Name="StopButton" Content="Pause" Command="{Binding Pause}" />
				<Button x:Name="NextButton" Content="Next" Command="{Binding Next}" />
				<Slider x:Name="VolumeSlider" Width="50" Value="{Binding Volume, Mode=OneTime}" Maximum="100" Minimum="0">
					<i:Interaction.Triggers>
						<i:EventTrigger EventName="ValueChanged">
                            <i:InvokeCommandAction Command="{Binding ChangeVolume}" CommandParameter="{Binding Value, ElementName=VolumeSlider}"/>
						</i:EventTrigger>
					</i:Interaction.Triggers>
				</Slider>
			</StackPanel>
		</StackPanel>
	</Grid>
</UserControl>